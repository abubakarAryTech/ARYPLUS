import React from 'react';

const LiveEventSlider = ({ type, title }) => {
  return (
    <div className="live-event-slider">
      {/* Placeholder for live event slider */}
      <h2>{title}</h2>
      <p>Type: {type}</p>
    </div>
  );
};

export default LiveEventSlider;